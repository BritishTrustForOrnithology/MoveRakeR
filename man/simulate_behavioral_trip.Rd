% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_behavioural_trip.R
\name{simulate_behavioral_trip}
\alias{simulate_behavioral_trip}
\alias{simulate_smooth_crw_bridge}
\alias{sim_data}
\title{Simulate animal movement trips with correlated random walks}
\usage{
simulate_behavioral_trip(
  n,
  hub_lat,
  hub_lon,
  rho_explore = 0.9,
  rho_forage = 0.3,
  rho_return = 0.8,
  step_explore = 0.05,
  step_forage = 0.01,
  step_return = 0.03,
  min_return_steps = 10
)

simulate_smooth_crw_bridge(
  n,
  hub_lat,
  hub_lon,
  step_mean = 0.05,
  step_sd = 0.02,
  rho = 0.9
)

sim_data(
  hub_lat = 40,
  hub_lon = 0,
  dt = 300,
  animals = c("A001", "B002", "C003"),
  points_per_trip = 50,
  n_trips = 5,
  seed = 39
)
}
\arguments{
\item{n}{Integer. Number of locations to simulate.}

\item{hub_lat}{Numeric, latitude of the central hub, within \code{sim_data()} default = 40.}

\item{hub_lon}{Numeric, longitude of the central hub, within \code{sim_data()} default = 0.}

\item{rho_explore}{Numeric (0–1). Correlation in turning angle parameter during exploration (higher values = straighter movement).}

\item{rho_forage}{Numeric (0–1). Correlation in turning angle parameter during foraging (lower values = more tortuous movement).}

\item{rho_return}{Numeric (0–1). Correlation in turning parameter during return phase.}

\item{step_explore}{Numeric. Mean step length during exploration.}

\item{step_forage}{Numeric. Mean step length during foraging.}

\item{step_return}{Numeric. Mean step length during return.}

\item{min_return_steps}{Integer. Minimum number of steps allowed during return phase.}

\item{step_mean}{Numeric. Mean step length for \code{simulate_smooth_crw_bridge()}.}

\item{step_sd}{Numeric. Standard deviation of step length for \code{simulate_smooth_crw_bridge()}.}

\item{rho}{Numeric (0–1). Correlation parameter for \code{simulate_smooth_crw_bridge()}.}

\item{dt}{Numeric, time difference between consecutive GPS fixes, for \code{sim_data()}, default = c("A001", "B002", "C003").}

\item{animals}{Character, concatenated string for ID of animals, for \code{sim_data()}.}

\item{points_per_trip}{Integer, for number of points per trip to simulate, default 50, within \code{sim_data()}.}

\item{n_trips, }{Integer, number of trips to simulate, default = 5, within \code{sim_data()}.}

\item{seed}{Integer for seed to set for reproducibility, within \code{sim_data()}, default = 39.}
}
\value{
A \code{data.frame} with two columns:
\describe{
\item{latitude}{Simulated latitude positions.}
\item{longitude}{Simulated longitude positions.}
}
}
\description{
These functions simulate artificial movement trajectories of animals departing from and returning to a
central hub (e.g. a colony or roost), using correlated random walk models.
}
\details{
\code{simulate_behavioral_trip()} generates a trajectory with distinct behavioral
phases (exploration, foraging, return) and adaptive return dynamics.

#' Generates an animal movement track with exploratory, foraging, and return phases.
Step lengths and turning correlations differ by phase, and the return phase meanders
naturally towards the hub. Extra points are added if needed to reach the hub.

\code{simulate_smooth_crw_bridge()} generates a simpler correlated random walk trajectory that departs
from the hub, wanders, and is "bridged" back to the hub with spline smoothing.
}
\examples{
# Simulate a behavioral trip with default parameters
trip1 <- simulate_behavioral_trip(n = 100, hub_lat = 40, hub_lon = 0)
plot(trip1$longitude, trip1$latitude, type = "l")

# Simulate a smooth CRW trajectory
trip2 <- simulate_smooth_crw_bridge(n = 100, hub_lat = 40, hub_lon = 0)
lines(trip2$longitude, trip2$latitude, col = "red")

}
